---
import { apps } from '@/apps';
import { appPaths } from '@/lib/legal';
import AppLandingPage from '@/components/pages/AppLandingPage.astro';

export function getStaticPaths() {
  return appPaths(apps, 'ja');
}

const { app, locale } = Astro.props;
---

<AppLandingPage app={app} locale={locale} />
