---
import { apps } from '@/apps';
import { appPaths } from '@/lib/legal';
import AppPrivacyPage from '@/components/pages/AppPrivacyPage.astro';

export function getStaticPaths() {
  return appPaths(apps, 'ja');
}

const { app, locale } = Astro.props;
---

<AppPrivacyPage app={app} locale={locale} />
