---
import { apps } from '@/apps';
import { appPaths } from '@/lib/legal';
import AppSupportPage from '@/components/pages/AppSupportPage.astro';

export function getStaticPaths() {
  return appPaths(apps, 'ja');
}

const { app, locale } = Astro.props;
---

<AppSupportPage app={app} locale={locale} />
