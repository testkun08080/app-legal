---
import { apps } from '@/apps';
import { appPaths } from '@/lib/legal';
import AppTermsPage from '@/components/pages/AppTermsPage.astro';

export function getStaticPaths() {
  return appPaths(apps, 'en');
}

const { app, locale } = Astro.props;
---

<AppTermsPage app={app} locale={locale} />
